<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.microcol</groupId>
		<artifactId>microcol-root</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>
	<artifactId>microcol-dist</artifactId>
	<packaging>jar</packaging>
	<name>Distribution</name>
	<description>
		Module for generating distribution
	</description>

	<properties>
		<keystore.path>${basedir}/src/keystore/keystore_vs.jks</keystore.path>
		<keystore.store.password>12monkeys</keystore.store.password>
		<keystore.key.password>12monkeys</keystore.key.password>
		<keystore.key.alias>spacewar</keystore.key.alias>
	</properties>

	<build>
		<plugins>
			<!-- skip installing artifacts -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-install-plugin</artifactId>
				<version>2.5.2</version>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
			<!-- Skip tests -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.19.1</version>
				<configuration>
					<skipTests>true</skipTests>
				</configuration>
			</plugin>
			<!-- This plugin configuration just prevent jar plugin from generating 
				jar. -->
			<plugin>
				<artifactId>maven-jar-plugin</artifactId>
				<version>2.4</version>
				<executions>
					<execution>
						<id>default-jar</id>
						<phase>never</phase>
						<configuration>
							<finalName>unwanted</finalName>
							<classifier>unwanted</classifier>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>